import { createContext } from "react";
const notecontext=createContext();//note se related jo bhi states hai use hold krega taaki mere components kitne hi niche ho hierarchy me,mai unko saari states acces krwa paunga
export default notecontext;